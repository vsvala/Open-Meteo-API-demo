server {
# 5173;
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://server:3001;
  }

  location / {
    proxy_pass http://app:5173;
  }
}